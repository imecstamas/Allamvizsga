<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
                name="viewModel"
                type="com.allamvizsga.tamas.feature.walk.detail.WalkDetailViewModel"/>

    </data>

    <android.support.design.widget.CoordinatorLayout
            android:layout_width="match_parent"
            android:transitionGroup="false"
            android:animateLayoutChanges="true"
            app:snackbarState="@{viewModel.snackbarState}"
            android:layout_height="match_parent"
            tools:context=".feature.walk.detail.WalkDetailActivity">

        <android.support.design.widget.AppBarLayout
                android:layout_width="match_parent"
                android:transitionGroup="false"
                android:id="@+id/appbar"
                android:layout_height="200dp">

            <android.support.design.widget.CollapsingToolbarLayout
                    android:layout_width="match_parent"
                    android:transitionGroup="false"
                    app:layout_scrollFlags="scroll|exitUntilCollapsed"
                    app:title="@{viewModel.walk.title}"
                    app:expandedTitleTextAppearance="@style/TextAppearance.Design.CollapsingToolbar.Expanded.White"
                    app:contentScrim="?attr/colorPrimary"
                    android:layout_height="match_parent">

                <!-- Load the image from code in order to be able to postpone enter transition -->
                <ImageView
                        android:layout_width="match_parent"
                        app:layout_collapseMode="parallax"
                        android:scaleType="centerCrop"
                        android:transitionName="@string/image_transition"
                        android:layout_height="match_parent"
                        android:id="@+id/image_view"
                        tools:ignore="ContentDescription"/>

                <android.support.v7.widget.Toolbar
                        android:id="@+id/toolbar"
                        app:layout_collapseMode="pin"
                        android:layout_width="match_parent"
                        android:layout_height="?actionBarSize"/>

            </android.support.design.widget.CollapsingToolbarLayout>

        </android.support.design.widget.AppBarLayout>

        <android.support.v4.widget.NestedScrollView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <TextView
                    android:id="@+id/description"
                    android:text="@{viewModel.walk.description}"
                    android:layout_marginTop="@dimen/activity_vertical_margin"
                    android:padding="@dimen/base_line"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"/>

        </android.support.v4.widget.NestedScrollView>

        <android.support.design.widget.FloatingActionButton
                android:id="@+id/button"
                android:layout_width="wrap_content"
                android:enabled="@{viewModel.startEnabled}"
                android:src="@drawable/ic_play_arrow_black_24dp"
                android:visibility="gone"
                app:layout_anchor="@id/appbar"
                app:layout_anchorGravity="bottom|right|end"
                android:layout_height="wrap_content"/>

    </android.support.design.widget.CoordinatorLayout>
</layout>