<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.WalkList">
        <!--<item name="android:windowExitTransition">@android:transition/explode</item>-->
    </style>

    <style name="AppTheme.WalkDetail">
        <item name="android:windowSharedElementsUseOverlay">false</item>
        <item name="android:windowEnterTransition">@transition/walk_detail_enter</item>
        <item name="android:windowReturnTransition">@android:transition/slide_bottom</item>
    </style>

</resources>